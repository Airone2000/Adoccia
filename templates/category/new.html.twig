{% extends 'base.html.twig' %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('pictureUploader') }}
    {{ encore_entry_script_tags('categoryForm') }}
{% endblock %}

{% block title %}{{ 'collection.new.page.title'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'collection.new.title'|trans }}</h1>

    {{ include('category/_form.html.twig') }}

    <a href="{{ path('category.index') }}">{{ 'collection.new.back.link'|trans }}</a>
{% endblock %}
