<script>
    const endpoints =
    {
        'login'                 : "{{ url('app.login') }}",
        'addFormAreaToForm'     : "{{ url('form.addArea', {'id': ':id'}) }}",
        'sortFormAreas'         : "{{ url('form.sortAreas', {'id': ':id'}) }}",
        'deleteFormArea'        : "{{ url('formArea.delete', {'id': ':id'}) }}",
        'setFormAreaWidth'      : "{{ url('formArea.setWidth', {'id': ':id'}) }}",
        'widgetChangeType'      : "{{ url('widget.changeType', {'id': ':id'}) }}",
        'widgetSettingsView'    : "{{ url('widget.getSettingsView', {'id': ':id'}) }}",
        'widgetSetSetting'      : "{{ url('widget.setSetting', {'id': ':id'}) }}"
    };
</script>